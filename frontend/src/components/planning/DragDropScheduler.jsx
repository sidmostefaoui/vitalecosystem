// Planning avec drag & drop
const DragDropScheduler = ({ missions, onMissionMove }) => {
  return (
    <DndProvider backend={HTML5Backend}>
      <WeekView>
        {/* Interface drag & drop */}
      </WeekView>
    </DndProvider>
  );
}; 